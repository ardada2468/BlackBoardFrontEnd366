import _extends from "@babel/runtime/helpers/esm/extends";
import BrowserProtocol from 'farce/BrowserProtocol';
import React from 'react';
import createFarceRouter from './createFarceRouter';
import resolver from './resolver';
export default function createBrowserRouter(options) {
  const FarceRouter = createFarceRouter(Object.assign({}, options, {
    historyProtocol: new BrowserProtocol()
  }));

  function BrowserRouter(props) {
    return /*#__PURE__*/React.createElement(FarceRouter, _extends({
      resolver: resolver
    }, props));
  }

  return BrowserRouter;
}