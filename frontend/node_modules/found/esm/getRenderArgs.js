import _asyncIterator from "@babel/runtime/helpers/esm/asyncIterator";
import resolveRenderArgs from './resolveRenderArgs';
export default async function getRenderArgs(router, props) {
  let elements;
  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;

  var _iteratorError;

  try {
    for (var _iterator = _asyncIterator(resolveRenderArgs(router, props)), _step, _value; _step = await _iterator.next(), _iteratorNormalCompletion = _step.done, _value = await _step.value, !_iteratorNormalCompletion; _iteratorNormalCompletion = true) {// Nothing to do here. We just need the last value from the iterable.

      elements = _value;
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator.return != null) {
        await _iterator.return();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  return elements;
}